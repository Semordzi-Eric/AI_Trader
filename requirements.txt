# Core
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
PyYAML>=6.0
python-dateutil>=2.8

# ML
torch>=2.1
gymnasium>=0.29
stable-baselines3>=2.2
# NOTE (Windows / Python ≥ 3.13): hmmlearn builds from source and requires
# Microsoft C++ Build Tools. Install from https://visualstudio.microsoft.com/visual-cpp-build-tools/
# before running `pip install hmmlearn`. Use --no-regime flag to skip until installed.
hmmlearn>=0.3

# Boosting (meta-model)
lightgbm>=4.0

# Backtest / analytics
pyarrow>=14.0          # parquet I/O

# Dashboard
streamlit>=1.30
plotly>=5.18

# Live trading (Windows host with MT5 terminal installed — uncomment to enable):
# MetaTrader5>=5.0.45

# Dev / tests
pytest>=7.4
pytest-cov>=4.1
